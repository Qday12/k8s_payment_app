
# IMPORTANT: This is a template. Do NOT commit actual credentials to Git.
#
# Option 1: Create secret manually:
#   kubectl create secret generic payment-db-credentials \
#     --namespace finpay-prod \
#     --from-literal=PAYMENT_DB_URL='jdbc:postgresql://<RDS_ENDPOINT>:5432/payments' \
#     --from-literal=PAYMENT_DB_USER='payments_app' \
#     --from-literal=PAYMENT_DB_PASSWORD='<password>'
#
# Option 2: Use External Secrets Operator with AWS Secrets Manager
# Option 3: Use Sealed Secrets
#
# The template below uses placeholder values for documentation purposes only.
---
apiVersion: v1
kind: Secret
metadata:
  name: payment-db-credentials
  namespace: finpay-prod
  labels:
    app.kubernetes.io/name: finpay
    app.kubernetes.io/instance: finpay-prod
    app.kubernetes.io/component: database
    app.kubernetes.io/managed-by: kubectl
type: Opaque
stringData:
  # Replace with actual RDS endpoint
  PAYMENT_DB_URL: "jdbc:postgresql://finpay-db.xxxxxxxxxxxx.eu-central-1.rds.amazonaws.com:5432/payments"
  PAYMENT_DB_USER: "payments_app"
  # NEVER commit real passwords - use kubectl create secret or External Secrets
  PAYMENT_DB_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"
